
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<body>
    <div>
        @using (Html.BeginForm())
        {
            @Session.SessionID
            <br/>

            <label>Contador: </label>
            
            if (Session["contador"] != null)
            {

                <label>@Session["contador"]</label>

            }

            <p>
                <label>Usuario:</label>
                @if (Session["nombreUsuario"] != null)
                {

                
                <label>@Session["nombreUsuario"]</label>
                <input type="submit" value="Cerrar Sesion" name="boton" />
            
                }else
                {
                  
                    <input type="text" name="txtNombre" />
                    <input type="submit" value="Entrar" name="boton" />
                }
            </p>
                <input type="submit" value="+ 1" name="boton" />



        }
    </div>
</body>
